# ── Core web framework ────────────────────────────────────────────
flask>=2.3.0
gunicorn>=21.2.0git --version

# ── NLP / ML ──────────────────────────────────────────────────────
nltk>=3.8.0                  # tokenisation, stopwords, stemming, lemmatisation
scikit-learn>=1.3.0          # TfidfVectorizer, TruncatedSVD (LSA), cosine_similarity
scipy>=1.11.0                # sparse matrix operations

# ── Numeric ───────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0

# ── Sentence Transformers / BERT (Tier 1 — best quality) ─────────
# Install these for BERT-quality semantic matching.
# The app automatically falls back to LSA (Tier 2) if not available.
sentence-transformers>=2.2.0   # includes: torch, transformers, huggingface-hub

# ── Alternative: bare HuggingFace (lighter) ──────────────────────
# torch>=2.0.0
# transformers>=4.30.0

# ── Resume file parsing ───────────────────────────────────────────
pdfminer.six>=20221105       # PDF text extraction (best quality)
python-docx>=0.8.11          # DOCX / DOC parsing

# ── Optional extras ───────────────────────────────────────────────
# PyPDF2>=3.0.0              # fallback PDF parser (if pdfminer not installed)
